apiVersion: v1
kind: Service
metadata:
  name: vector-db-service
spec:
  type: NodePort
  selector:
    app: vector-db
  ports:
  - protocol: TCP
    port: 8000
    targetPort: 8000
    nodePort: 31000  # Optional: specify a port in the range 30000-32767